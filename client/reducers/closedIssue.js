import C from "../data/constants";
// import { addComment, deleteComment } from "../actions/closedIssue";

function closedIssue(state = [], action) {
  // switch (action.type) {
  //   case C.ADD_COMMENT:
  //     return addComment(state, action.payload);
  //   case C.REMOVE_COMMENT:
  //     return deleteComment(state, action.payload);
  //   default:
  //     return state;
  // }
  return state;
}
export default closedIssue;