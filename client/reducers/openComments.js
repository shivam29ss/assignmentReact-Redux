// import {incrementLikes} from "../actions/openComments"
import C from "../data/constants"

function openComments(state = [], action) {
  // switch (action.type) {
  //   case C.INCREMENT_LIKES:
  //     return incrementLikes(state, action.payload);
  //   default:
  //     return state;
  // }
return state;
}

export default openComments;
